--- a/net/minecraft/server/MinecraftServer.java
+++ b/net/minecraft/server/MinecraftServer.java
@@ -1298,7 +_,7 @@
             // Paper end - Add onboarding message for initial server start
             // Paper start - Improve outdated version checking
             if (System.getProperty("paper.disableStartupVersionCheck") == null && io.papermc.paper.configuration.GlobalConfiguration.get().updateChecker.enabled) {
-                CompletableFuture.runAsync(com.destroystokyo.paper.PaperVersionFetcher::getUpdateStatusStartupMessage, io.papermc.paper.util.MCUtil.ASYNC_EXECUTOR);
+                CompletableFuture.runAsync(org.embermc.ember.EmberVersionFetcher::logStartupStatus, io.papermc.paper.util.MCUtil.ASYNC_EXECUTOR); // Ember - our own fetcher; Paper's would compare against Paper builds
             }
             // Paper end - Improve outdated version checking
 
@@ -1614,6 +_,7 @@
         }
 
         this.server.spark.tickStart(); // Paper - spark
+        org.embermc.ember.profiler.EmberProfiler.tickStart(); // Ember - profiler: tick begins
         new com.destroystokyo.paper.event.server.ServerTickStartEvent(this.tickCount+1).callEvent(); // Paper - Server Tick Events
         this.tickCount++;
         this.tickRateManager.tick();
@@ -1630,6 +_,7 @@
             playerSaveInterval = this.autosavePeriod;
         }
         Profiler.get().push("save");
+        final long emberSave = org.embermc.ember.profiler.EmberProfiler.start(); // Ember - profiler
         final boolean fullSave = this.autosavePeriod > 0 && this.tickCount % this.autosavePeriod == 0;
         try {
             this.isSaving = true;
@@ -1647,6 +_,7 @@
         } finally {
             this.isSaving = false;
         }
+        org.embermc.ember.profiler.EmberProfiler.end(org.embermc.ember.profiler.Phase.AUTOSAVE, emberSave); // Ember - profiler
         Profiler.get().pop();
         // Paper end - Incremental chunk and player saving
 
@@ -1657,6 +_,7 @@
         long remaining = this.nextTickTimeNanos - endTime;
         new com.destroystokyo.paper.event.server.ServerTickEndEvent(this.tickCount, ((double)(endTime - this.currentTickStart) / 1000000D), remaining).callEvent();
         // Paper end - Server Tick Events
+        org.embermc.ember.profiler.EmberProfiler.tickEnd(); // Ember - profiler: tick ends, spike watchdog runs
         this.server.spark.tickEnd(((double)(endTime - this.currentTickStart) / 1000000D)); // Paper - spark
         profiler.push("tallying");
         long tickTime = Util.getNanos() - nano;
@@ -1764,7 +_,9 @@
     protected void tickChildren(final BooleanSupplier haveTime) {
         ProfilerFiller profiler = Profiler.get();
         this.getPlayerList().getPlayers().forEach(playerx -> playerx.connection.suspendFlushing());
+        final long emberScheduler = org.embermc.ember.profiler.EmberProfiler.start(); // Ember - profiler
         this.server.getScheduler().mainThreadHeartbeat(); // CraftBukkit
+        org.embermc.ember.profiler.EmberProfiler.end(org.embermc.ember.profiler.Phase.SCHEDULER, emberScheduler); // Ember - profiler
         // Paper start - optimise Folia entity scheduler
         ((io.papermc.paper.threadedregions.scheduler.FoliaGlobalRegionScheduler)org.bukkit.Bukkit.getGlobalRegionScheduler()).tick();
         for (io.papermc.paper.threadedregions.EntityScheduler scheduler : this.entitySchedulerTickList.getAllSchedulers()) {
@@ -1820,7 +_,9 @@
             profiler.push("tick");
 
             try {
+                final long emberWorld = org.embermc.ember.profiler.EmberProfiler.start(); // Ember - profiler
                 level.tick(haveTime);
+                org.embermc.ember.profiler.EmberProfiler.worldTickEnd(level, emberWorld); // Ember - profiler
             } catch (Throwable t) {
                 CrashReport report = CrashReport.forThrowable(t, "Exception ticking world");
                 level.fillReportDetails(report);
@@ -1871,7 +_,9 @@
     }
 
     protected void tickConnection() {
+        final long emberConnections = org.embermc.ember.profiler.EmberProfiler.start(); // Ember - profiler
         this.getConnection().tick();
+        org.embermc.ember.profiler.EmberProfiler.end(org.embermc.ember.profiler.Phase.CONNECTIONS, emberConnections); // Ember - profiler
     }
 
     public void forceGameTimeSynchronization() {
